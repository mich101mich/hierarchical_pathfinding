version: 2
jobs:
  build:
    docker:
      - image: circleci/rust:1.31.0-stretch
    steps:
      - checkout
      - restore_cache:
          key: build-cache
      - run:
          name: Test
          command: cargo test
      - save_cache:
          key: build-cache
          paths:
            - target